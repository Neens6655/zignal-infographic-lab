export const metadata = {
  title: 'ZGNAL Chat — Conversational Infographic Generator',
  description: 'Generate publication-grade infographics through a simple chat interface.',
};

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
